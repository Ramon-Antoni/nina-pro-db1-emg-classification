# nina-pro-db1-emg-classification
This repository contains two different models (a deep echo state network and a temporal convolutional network) that solve the task of assigning an electromyography signal to the corresponding gesture, with an accuracy of ~72% for the deep EMG and ~75% for the TCN.
